"""giteaapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from giteaapi.api_helper import APIHelper


class Cron(object):
    """Implementation of the 'Cron' model.

    Cron represents a Cron task

    Attributes:
        exec_times (int): The total number of times this cron task has been executed
        name (str): The name of the cron task
        next (datetime): The next scheduled execution time
        prev (datetime): The previous execution time
        schedule (str): The cron schedule expression (e.g., "0 0 * * *")
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "exec_times": "exec_times",
        "name": "name",
        "next": "next",
        "prev": "prev",
        "schedule": "schedule",
    }

    _optionals = [
        "exec_times",
        "name",
        "next",
        "prev",
        "schedule",
    ]

    def __init__(
        self,
        exec_times=APIHelper.SKIP,
        name=APIHelper.SKIP,
        next=APIHelper.SKIP,
        prev=APIHelper.SKIP,
        schedule=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Cron instance."""
        # Initialize members of the class
        if exec_times is not APIHelper.SKIP:
            self.exec_times = exec_times
        if name is not APIHelper.SKIP:
            self.name = name
        if next is not APIHelper.SKIP:
            self.next =\
                 APIHelper.apply_datetime_converter(
                next, APIHelper.RFC3339DateTime)\
                 if next else None
        if prev is not APIHelper.SKIP:
            self.prev =\
                 APIHelper.apply_datetime_converter(
                prev, APIHelper.RFC3339DateTime)\
                 if prev else None
        if schedule is not APIHelper.SKIP:
            self.schedule = schedule

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        exec_times =\
            dictionary.get("exec_times")\
            if dictionary.get("exec_times")\
                else APIHelper.SKIP
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else APIHelper.SKIP
        next = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("next")).datetime\
            if dictionary.get("next") else APIHelper.SKIP
        prev = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("prev")).datetime\
            if dictionary.get("prev") else APIHelper.SKIP
        schedule =\
            dictionary.get("schedule")\
            if dictionary.get("schedule")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(exec_times,
                   name,
                   next,
                   prev,
                   schedule,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _exec_times=(
            self.exec_times
            if hasattr(self, "exec_times")
            else None
        )
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _next=(
            self.next
            if hasattr(self, "next")
            else None
        )
        _prev=(
            self.prev
            if hasattr(self, "prev")
            else None
        )
        _schedule=(
            self.schedule
            if hasattr(self, "schedule")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"exec_times={_exec_times!r}, "
            f"name={_name!r}, "
            f"next={_next!r}, "
            f"prev={_prev!r}, "
            f"schedule={_schedule!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _exec_times=(
            self.exec_times
            if hasattr(self, "exec_times")
            else None
        )
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _next=(
            self.next
            if hasattr(self, "next")
            else None
        )
        _prev=(
            self.prev
            if hasattr(self, "prev")
            else None
        )
        _schedule=(
            self.schedule
            if hasattr(self, "schedule")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"exec_times={_exec_times!s}, "
            f"name={_name!s}, "
            f"next={_next!s}, "
            f"prev={_prev!s}, "
            f"schedule={_schedule!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
