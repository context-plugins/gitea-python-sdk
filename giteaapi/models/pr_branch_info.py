"""giteaapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from giteaapi.api_helper import APIHelper
from giteaapi.models.repository import Repository


class PrBranchInfo(object):
    """Implementation of the 'PRBranchInfo' model.

    PRBranchInfo information about a branch

    Attributes:
        label (str): The display name of the branch
        ref (str): The git reference of the branch
        repo (Repository): Repository represents a repository
        repo_id (int): The unique identifier of the repository
        sha (str): The commit SHA of the branch head
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "label": "label",
        "ref": "ref",
        "repo": "repo",
        "repo_id": "repo_id",
        "sha": "sha",
    }

    _optionals = [
        "label",
        "ref",
        "repo",
        "repo_id",
        "sha",
    ]

    def __init__(
        self,
        label=APIHelper.SKIP,
        ref=APIHelper.SKIP,
        repo=APIHelper.SKIP,
        repo_id=APIHelper.SKIP,
        sha=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a PrBranchInfo instance."""
        # Initialize members of the class
        if label is not APIHelper.SKIP:
            self.label = label
        if ref is not APIHelper.SKIP:
            self.ref = ref
        if repo is not APIHelper.SKIP:
            self.repo = repo
        if repo_id is not APIHelper.SKIP:
            self.repo_id = repo_id
        if sha is not APIHelper.SKIP:
            self.sha = sha

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        label =\
            dictionary.get("label")\
            if dictionary.get("label")\
                else APIHelper.SKIP
        ref =\
            dictionary.get("ref")\
            if dictionary.get("ref")\
                else APIHelper.SKIP
        repo =\
            Repository.from_dictionary(
                dictionary.get("repo"))\
                if "repo" in dictionary.keys()\
                else APIHelper.SKIP
        repo_id =\
            dictionary.get("repo_id")\
            if dictionary.get("repo_id")\
                else APIHelper.SKIP
        sha =\
            dictionary.get("sha")\
            if dictionary.get("sha")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(label,
                   ref,
                   repo,
                   repo_id,
                   sha,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _label=(
            self.label
            if hasattr(self, "label")
            else None
        )
        _ref=(
            self.ref
            if hasattr(self, "ref")
            else None
        )
        _repo=(
            self.repo
            if hasattr(self, "repo")
            else None
        )
        _repo_id=(
            self.repo_id
            if hasattr(self, "repo_id")
            else None
        )
        _sha=(
            self.sha
            if hasattr(self, "sha")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"label={_label!r}, "
            f"ref={_ref!r}, "
            f"repo={_repo!r}, "
            f"repo_id={_repo_id!r}, "
            f"sha={_sha!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _label=(
            self.label
            if hasattr(self, "label")
            else None
        )
        _ref=(
            self.ref
            if hasattr(self, "ref")
            else None
        )
        _repo=(
            self.repo
            if hasattr(self, "repo")
            else None
        )
        _repo_id=(
            self.repo_id
            if hasattr(self, "repo_id")
            else None
        )
        _sha=(
            self.sha
            if hasattr(self, "sha")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"label={_label!s}, "
            f"ref={_ref!s}, "
            f"repo={_repo!s}, "
            f"repo_id={_repo_id!s}, "
            f"sha={_sha!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
