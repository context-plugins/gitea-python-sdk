"""giteaapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.or_auth_group import (
    Or,
)
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from giteaapi.api_helper import APIHelper
from giteaapi.apis.base_api import BaseApi
from giteaapi.configuration import Server
from giteaapi.exceptions.api_exception import (
    ApiException,
)
from giteaapi.http.http_method_enum import HttpMethodEnum
from giteaapi.models.package import Package
from giteaapi.models.package_file import PackageFile


class PackageApi(BaseApi):
    """A Controller to access Endpoints in the giteaapi API."""

    def __init__(self, config):
        """Initialize PackageApi object."""
        super(PackageApi, self).__init__(config)

    def list_packages(self,
                      owner,
                      page=None,
                      limit=None,
                      mtype=None,
                      q=None):
        """Perform a GET request to /packages/{owner}.

        Gets all packages of an owner

        Args:
            owner (str): owner of the packages
            page (int, optional): page number of results to return (1-based)
            limit (int, optional): page size of results
            mtype (Type4, optional): package type filter
            q (str, optional): name filter

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. PackageList

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("page")
                .value(page))
            .query_param(Parameter()
                .key("limit")
                .value(limit))
            .query_param(Parameter()
                .key("type")
                .value(mtype))
            .query_param(Parameter()
                .key("q")
                .value(q))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Package.from_dictionary)
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def list_package_versions(self,
                              owner,
                              mtype,
                              name,
                              page=None,
                              limit=None):
        """Perform a GET request to /packages/{owner}/{type}/{name}.

        Gets all versions of a package

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package
            page (int, optional): page number of results to return (1-based)
            limit (int, optional): page size of results

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. PackageList

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("page")
                .value(page))
            .query_param(Parameter()
                .key("limit")
                .value(limit))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Package.from_dictionary)
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def delete_package(self,
                       owner,
                       mtype,
                       name):
        """Perform a DELETE request to /packages/{owner}/{type}/{name}.

        Delete a package

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. APIEmpty is an empty
                response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def get_latest_package_version(self,
                                   owner,
                                   mtype,
                                   name):
        """Perform a GET request to /packages/{owner}/{type}/{name}/-/latest.

        Gets the latest version of a package

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Package

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}/-/latest")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Package.from_dictionary)
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def link_package(self,
                     owner,
                     mtype,
                     name,
                     repo_name):
        """Perform a POST request to
        /packages/{owner}/{type}/{name}/-/link/{repo_name}.

        Link a package to a repository

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package
            repo_name (str): name of the repository to link.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. APIEmpty is an empty
                response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}/-/link/{repo_name}")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("repo_name")
                .value(repo_name)
                .is_required(True)
                .should_encode(True))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def unlink_package(self,
                       owner,
                       mtype,
                       name):
        """Perform a POST request to /packages/{owner}/{type}/{name}/-/unlink.

        Unlink a package from a repository

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. APIEmpty is an empty
                response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}/-/unlink")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def get_package(self,
                    owner,
                    mtype,
                    name,
                    version):
        """Perform a GET request to /packages/{owner}/{type}/{name}/{version}.

        Gets a package

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package
            version (str): version of the package

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Package

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}/{version}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("version")
                .value(version)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Package.from_dictionary)
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def delete_package_version(self,
                               owner,
                               mtype,
                               name,
                               version):
        """Perform a DELETE request to
        /packages/{owner}/{type}/{name}/{version}.

        Delete a package version

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package
            version (str): version of the package

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. APIEmpty is an empty
                response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}/{version}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("version")
                .value(version)
                .is_required(True)
                .should_encode(True))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()

    def list_package_files(self,
                           owner,
                           mtype,
                           name,
                           version):
        """Perform a GET request to
        /packages/{owner}/{type}/{name}/{version}/files.

        Gets all files of a package

        Args:
            owner (str): owner of the package
            mtype (str): type of the package
            name (str): name of the package
            version (str): version of the package

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. PackageFileList

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.SERVER1)
            .path("/packages/{owner}/{type}/{name}/{version}/files")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("owner")
                .value(owner)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("type")
                .value(mtype)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("name")
                .value(name)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("version")
                .value(version)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Or(Single("BasicAuth"), Single("Token"),
                Single("AccessToken"), Single("AuthorizationHeaderToken"),
                Single("SudoParam"), Single("SudoHeader"), Single("TOTPHeader"))),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(PackageFile.from_dictionary)
            .is_api_response(True)
            .local_error("404",
                "APINotFound is a not found empty response",
                ApiException),
        ).execute()
