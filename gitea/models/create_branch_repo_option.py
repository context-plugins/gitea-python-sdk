"""gitea.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from gitea.api_helper import APIHelper


class CreateBranchRepoOption(object):
    """Implementation of the 'CreateBranchRepoOption' model.

    CreateBranchRepoOption options when creating a branch in a repository

    Attributes:
        new_branch_name (str): Name of the branch to create
        old_branch_name (str): Deprecated: true Name of the old branch to create from
        old_ref_name (str): Name of the old branch/tag/commit to create from
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "new_branch_name": "new_branch_name",
        "old_branch_name": "old_branch_name",
        "old_ref_name": "old_ref_name",
    }

    _optionals = [
        "old_branch_name",
        "old_ref_name",
    ]

    def __init__(
        self,
        new_branch_name=None,
        old_branch_name=APIHelper.SKIP,
        old_ref_name=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a CreateBranchRepoOption instance."""
        # Initialize members of the class
        self.new_branch_name = new_branch_name
        if old_branch_name is not APIHelper.SKIP:
            self.old_branch_name = old_branch_name
        if old_ref_name is not APIHelper.SKIP:
            self.old_ref_name = old_ref_name

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        new_branch_name =\
            dictionary.get("new_branch_name")\
            if dictionary.get("new_branch_name")\
                else None
        old_branch_name =\
            dictionary.get("old_branch_name")\
            if dictionary.get("old_branch_name")\
                else APIHelper.SKIP
        old_ref_name =\
            dictionary.get("old_ref_name")\
            if dictionary.get("old_ref_name")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(new_branch_name,
                   old_branch_name,
                   old_ref_name,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _new_branch_name=self.new_branch_name
        _old_branch_name=(
            self.old_branch_name
            if hasattr(self, "old_branch_name")
            else None
        )
        _old_ref_name=(
            self.old_ref_name
            if hasattr(self, "old_ref_name")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"new_branch_name={_new_branch_name!r}, "
            f"old_branch_name={_old_branch_name!r}, "
            f"old_ref_name={_old_ref_name!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _new_branch_name=self.new_branch_name
        _old_branch_name=(
            self.old_branch_name
            if hasattr(self, "old_branch_name")
            else None
        )
        _old_ref_name=(
            self.old_ref_name
            if hasattr(self, "old_ref_name")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"new_branch_name={_new_branch_name!s}, "
            f"old_branch_name={_old_branch_name!s}, "
            f"old_ref_name={_old_ref_name!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
