"""gitea.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from gitea.api_helper import APIHelper


class PushMirror(object):
    """Implementation of the 'PushMirror' model.

    PushMirror represents information of a push mirror

    Attributes:
        created (datetime): The model property of type datetime.
        interval (str): The sync interval for automatic updates
        last_error (str): The last error message encountered during sync
        last_update (datetime): The model property of type datetime.
        remote_address (str): The remote repository URL being mirrored to
        remote_name (str): The name of the remote in the git configuration
        repo_name (str): The name of the source repository
        sync_on_commit (bool): Whether to sync on every commit
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "created": "created",
        "interval": "interval",
        "last_error": "last_error",
        "last_update": "last_update",
        "remote_address": "remote_address",
        "remote_name": "remote_name",
        "repo_name": "repo_name",
        "sync_on_commit": "sync_on_commit",
    }

    _optionals = [
        "created",
        "interval",
        "last_error",
        "last_update",
        "remote_address",
        "remote_name",
        "repo_name",
        "sync_on_commit",
    ]

    def __init__(
        self,
        created=APIHelper.SKIP,
        interval=APIHelper.SKIP,
        last_error=APIHelper.SKIP,
        last_update=APIHelper.SKIP,
        remote_address=APIHelper.SKIP,
        remote_name=APIHelper.SKIP,
        repo_name=APIHelper.SKIP,
        sync_on_commit=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a PushMirror instance."""
        # Initialize members of the class
        if created is not APIHelper.SKIP:
            self.created =\
                 APIHelper.apply_datetime_converter(
                created, APIHelper.RFC3339DateTime)\
                 if created else None
        if interval is not APIHelper.SKIP:
            self.interval = interval
        if last_error is not APIHelper.SKIP:
            self.last_error = last_error
        if last_update is not APIHelper.SKIP:
            self.last_update =\
                 APIHelper.apply_datetime_converter(
                last_update, APIHelper.RFC3339DateTime)\
                 if last_update else None
        if remote_address is not APIHelper.SKIP:
            self.remote_address = remote_address
        if remote_name is not APIHelper.SKIP:
            self.remote_name = remote_name
        if repo_name is not APIHelper.SKIP:
            self.repo_name = repo_name
        if sync_on_commit is not APIHelper.SKIP:
            self.sync_on_commit = sync_on_commit

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        created = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("created")).datetime\
            if dictionary.get("created") else APIHelper.SKIP
        interval =\
            dictionary.get("interval")\
            if dictionary.get("interval")\
                else APIHelper.SKIP
        last_error =\
            dictionary.get("last_error")\
            if dictionary.get("last_error")\
                else APIHelper.SKIP
        last_update = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("last_update")).datetime\
            if dictionary.get("last_update") else APIHelper.SKIP
        remote_address =\
            dictionary.get("remote_address")\
            if dictionary.get("remote_address")\
                else APIHelper.SKIP
        remote_name =\
            dictionary.get("remote_name")\
            if dictionary.get("remote_name")\
                else APIHelper.SKIP
        repo_name =\
            dictionary.get("repo_name")\
            if dictionary.get("repo_name")\
                else APIHelper.SKIP
        sync_on_commit =\
            dictionary.get("sync_on_commit")\
            if "sync_on_commit" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(created,
                   interval,
                   last_error,
                   last_update,
                   remote_address,
                   remote_name,
                   repo_name,
                   sync_on_commit,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _created=(
            self.created
            if hasattr(self, "created")
            else None
        )
        _interval=(
            self.interval
            if hasattr(self, "interval")
            else None
        )
        _last_error=(
            self.last_error
            if hasattr(self, "last_error")
            else None
        )
        _last_update=(
            self.last_update
            if hasattr(self, "last_update")
            else None
        )
        _remote_address=(
            self.remote_address
            if hasattr(self, "remote_address")
            else None
        )
        _remote_name=(
            self.remote_name
            if hasattr(self, "remote_name")
            else None
        )
        _repo_name=(
            self.repo_name
            if hasattr(self, "repo_name")
            else None
        )
        _sync_on_commit=(
            self.sync_on_commit
            if hasattr(self, "sync_on_commit")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"created={_created!r}, "
            f"interval={_interval!r}, "
            f"last_error={_last_error!r}, "
            f"last_update={_last_update!r}, "
            f"remote_address={_remote_address!r}, "
            f"remote_name={_remote_name!r}, "
            f"repo_name={_repo_name!r}, "
            f"sync_on_commit={_sync_on_commit!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _created=(
            self.created
            if hasattr(self, "created")
            else None
        )
        _interval=(
            self.interval
            if hasattr(self, "interval")
            else None
        )
        _last_error=(
            self.last_error
            if hasattr(self, "last_error")
            else None
        )
        _last_update=(
            self.last_update
            if hasattr(self, "last_update")
            else None
        )
        _remote_address=(
            self.remote_address
            if hasattr(self, "remote_address")
            else None
        )
        _remote_name=(
            self.remote_name
            if hasattr(self, "remote_name")
            else None
        )
        _repo_name=(
            self.repo_name
            if hasattr(self, "repo_name")
            else None
        )
        _sync_on_commit=(
            self.sync_on_commit
            if hasattr(self, "sync_on_commit")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"created={_created!s}, "
            f"interval={_interval!s}, "
            f"last_error={_last_error!s}, "
            f"last_update={_last_update!s}, "
            f"remote_address={_remote_address!s}, "
            f"remote_name={_remote_name!s}, "
            f"repo_name={_repo_name!s}, "
            f"sync_on_commit={_sync_on_commit!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
