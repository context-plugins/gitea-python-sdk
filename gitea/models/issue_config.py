"""gitea.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from gitea.api_helper import APIHelper
from gitea.models.issue_config_contact_link import (
    IssueConfigContactLink,
)


class IssueConfig(object):
    """Implementation of the 'IssueConfig' model.

    Attributes:
        blank_issues_enabled (bool): The model property of type bool.
        contact_links (List[IssueConfigContactLink]): The model property of type
            List[IssueConfigContactLink].
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "blank_issues_enabled": "blank_issues_enabled",
        "contact_links": "contact_links",
    }

    _optionals = [
        "blank_issues_enabled",
        "contact_links",
    ]

    def __init__(
        self,
        blank_issues_enabled=APIHelper.SKIP,
        contact_links=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a IssueConfig instance."""
        # Initialize members of the class
        if blank_issues_enabled is not APIHelper.SKIP:
            self.blank_issues_enabled = blank_issues_enabled
        if contact_links is not APIHelper.SKIP:
            self.contact_links = contact_links

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        blank_issues_enabled =\
            dictionary.get("blank_issues_enabled")\
            if "blank_issues_enabled" in dictionary.keys()\
                else APIHelper.SKIP
        contact_links = None
        if dictionary.get("contact_links") is not None:
            contact_links = [
                IssueConfigContactLink.from_dictionary(x)
                    for x in dictionary.get("contact_links")
            ]
        else:
            contact_links = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(blank_issues_enabled,
                   contact_links,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _blank_issues_enabled=(
            self.blank_issues_enabled
            if hasattr(self, "blank_issues_enabled")
            else None
        )
        _contact_links=(
            self.contact_links
            if hasattr(self, "contact_links")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"blank_issues_enabled={_blank_issues_enabled!r}, "
            f"contact_links={_contact_links!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _blank_issues_enabled=(
            self.blank_issues_enabled
            if hasattr(self, "blank_issues_enabled")
            else None
        )
        _contact_links=(
            self.contact_links
            if hasattr(self, "contact_links")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"blank_issues_enabled={_blank_issues_enabled!s}, "
            f"contact_links={_contact_links!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
