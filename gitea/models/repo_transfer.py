"""gitea.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from gitea.api_helper import APIHelper
from gitea.models.team import Team
from gitea.models.user import User


class RepoTransfer(object):
    """Implementation of the 'RepoTransfer' model.

    RepoTransfer represents a pending repo transfer

    Attributes:
        doer (User): User represents a user
        recipient (User): User represents a user
        teams (List[Team]): The model property of type List[Team].
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "doer": "doer",
        "recipient": "recipient",
        "teams": "teams",
    }

    _optionals = [
        "doer",
        "recipient",
        "teams",
    ]

    def __init__(
        self,
        doer=APIHelper.SKIP,
        recipient=APIHelper.SKIP,
        teams=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a RepoTransfer instance."""
        # Initialize members of the class
        if doer is not APIHelper.SKIP:
            self.doer = doer
        if recipient is not APIHelper.SKIP:
            self.recipient = recipient
        if teams is not APIHelper.SKIP:
            self.teams = teams

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        doer =\
            User.from_dictionary(
                dictionary.get("doer"))\
                if "doer" in dictionary.keys()\
                else APIHelper.SKIP
        recipient =\
            User.from_dictionary(
                dictionary.get("recipient"))\
                if "recipient" in dictionary.keys()\
                else APIHelper.SKIP
        teams = None
        if dictionary.get("teams") is not None:
            teams = [
                Team.from_dictionary(x)
                    for x in dictionary.get("teams")
            ]
        else:
            teams = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(doer,
                   recipient,
                   teams,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _doer=(
            self.doer
            if hasattr(self, "doer")
            else None
        )
        _recipient=(
            self.recipient
            if hasattr(self, "recipient")
            else None
        )
        _teams=(
            self.teams
            if hasattr(self, "teams")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"doer={_doer!r}, "
            f"recipient={_recipient!r}, "
            f"teams={_teams!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _doer=(
            self.doer
            if hasattr(self, "doer")
            else None
        )
        _recipient=(
            self.recipient
            if hasattr(self, "recipient")
            else None
        )
        _teams=(
            self.teams
            if hasattr(self, "teams")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"doer={_doer!s}, "
            f"recipient={_recipient!s}, "
            f"teams={_teams!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
